language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t raedlerdan/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true raedlerdan/docker-react npm run test -- --coverage